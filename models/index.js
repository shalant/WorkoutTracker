// collect models and export them (object filled with different models)
const Workout = require("./workout.js");
const Exercise = require('./exercise')



module.exports= {
    Workout,
    Exercise
};